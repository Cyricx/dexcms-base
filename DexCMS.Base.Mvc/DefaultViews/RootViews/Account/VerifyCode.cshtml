@model DexCMS.Base.Mvc.Models.VerifyCodeViewModel


@using (Html.BeginForm("VerifyCode", "Account", new { ReturnUrl = Model.ReturnUrl }, 
    FormMethod.Post, new { @class = "form-box", role = "form" })) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "")
    @Html.Hidden("provider", @Model.Provider)
    <h3>@ViewBag.Status</h3>

    <div class="form-item">
        @Html.LabelFor(m => m.Code, new { @class = "form-lbl" })
        <div>
            @Html.TextBoxFor(m => m.Code)
        </div>
    </div>
    <div class="form-item">
        @Html.LabelFor(m => m.RememberBrowser, new { @class = "form-lbl" })
        <div>
            @Html.CheckBoxFor(m => m.RememberBrowser)
        </div>
    </div>
    <div class="form-btn">
        <input type="submit" value="Submit" />
    </div>
}

